# Nebula Desktop

Tauri + React desktop application. Not yet set up.

## Setup (when ready)

```bash
cd apps/desktop
pnpm add -D @tauri-apps/cli
pnpm tauri init
```

Then add React, Vite, and UI dependencies as needed.
